<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MANAGER - Widok Zarządcy</title>
    <link rel="stylesheet" href="/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <style>
        body { background-color: #f4f4f4; }
        .card { transition: 0.3s; border: none; border-radius: 12px; box-shadow: 0 4px 6px rgba(0,0,0,0.05); }
        .card:hover { transform: translateY(-5px); box-shadow: 0 8px 15px rgba(0,0,0,0.1); }
        .navbar { margin-bottom: 30px; }
        .building-icon { font-size: 2.5rem; color: #007bff; }
    </style>
</head>

<body>
    <% if (userRole === 'admin') { %>
        <div class="bg-info text-white text-center py-1 shadow-sm">
            <i class="bi bi-eye me-2"></i> Admin mode
        </div>
    <% } %>

    <nav class="navbar navbar-dark bg-dark shadow">
        <div class="container-fluid">
            <% if (userRole === 'admin') { %>
                <span class="navbar-brand fw-bold">Widok Admina</span>
                <a href="/admin" class="btn btn-outline-light btn-sm">
                    <i class="bi bi-arrow-left-circle"></i> Powrót
                </a>
            <% } else if (userRole === 'manager') { %>
                <span class="navbar-brand fw-bold">Widok Zarządcy</span>
                <a href="/logout" class="btn btn-outline-danger btn-sm">
                    <i class="bi bi-box-arrow-right"></i> Wyloguj
                </a>
            <% } else { %>
                <span class="navbar-brand fw-bold">Widok Usera</span>
                <a href="/logout" class="btn btn-outline-danger btn-sm">
                    <i class="bi bi-box-arrow-right"></i> Wyloguj
                </a>
            <% } %>
        </div>
    </nav>

    <div class="card mb-3 shadow-sm border-0 bg-white" style="max-width: 350px; position: sticky; top: 10px; z-index: 1000; margin-left: 15px; margin-top: 20px;">
        <div class="card-body d-flex align-items-center p-2">
            <div class="flex-shrink-0">
                <div class="rounded-circle bg-primary d-flex align-items-center justify-content-center text-white" style="width: 40px; height: 40px;">
                    <i class="bi bi-person-badge fs-4"></i>
                </div>
            </div>
            <div class="flex-grow-1 ms-3">
                <h6 class="mb-0 fw-bold"><%= currentUser.name %> <%= currentUser.surname %></h6>
                <small class="text-muted" style="font-size: 0.75rem;">
                    <i class="bi bi-envelope me-1"></i><%= currentUser.email %>
                </small>
            </div>
            <div class="ms-2">
                <span class="badge bg-success-subtle text-success border border-success-subtle rounded-pill text-capitalize" style="font-size: 0.65rem;"><%= currentUser.role %></span>
            </div>
        </div>
    </div>

    <div class="container">
        <div class="row mb-4">
            <% if (userRole === 'user') { %>
                <div class="col">
                    <h2 class="fw-light">Wybierz budynek, aby view jego strukturą.</h2>
                </div>
            <% } else { %>
                <div class="col">
                    <h2 class="fw-light">Wybierz budynek, aby zarządzać jego strukturą.</h2>
                </div>
            <% } %>
        </div>
        <% if (userRole === 'user') { %>
            <div class="container">
                <div class="row row-cols-1 row-cols-md-3 g-4">
                    <% if (buildings && buildings.length > 0) { %>
                        <% buildings.forEach(building => { %>
                            <div class="col">
                                <div class="card h-100 p-3">
                                    <div class="card-body text-center">
                                        <div class="building-icon mb-3">
                                            <i class="bi bi-buildings text-success"></i>
                                        </div>
                                        <h5 class="card-title fw-bold"><%= building.name %></h5>
                                        <p class="card-text text-muted small mb-4">
                                            <i class="bi bi-geo-alt"></i> <%= building.address %>
                                        </p>
                                
                                        <a href="/buildings/building/<%= building.id %>" class="btn btn-success w-100 shadow-sm">
                                            <i class="bi bi-search me-1"></i> Zobacz szczegóły
                                        </a>
                                    </div>
                                </div>
                            </div>
                        <% }) %>
                    <% } else { %>
                        <div class="col-12 text-center mt-5">
                            <div class="alert alert-warning">
                                <i class="bi bi-exclamation-triangle me-2"></i> Brak dostępnych budynków do wyświetlenia.
                            </div>
                        </div>
                    <% } %>
                </div>
            </div>
        <% } else { %>
        <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
            <% if (buildings && buildings.length > 0) { %>
                <% buildings.forEach(building => { %>
                    <div class="col">
                        <div class="card h-100 p-3">
                            <div class="card-body text-center">
                                <div class="building-icon mb-3">
                                    <i class="bi bi-buildings-fill"></i>
                                </div>
                                <h5 class="card-title fw-bold"><%= building.name %></h5>
                                <p class="card-text text-muted small mb-4">
                                    <i class="bi bi-geo-alt"></i> <%= building.address %>
                                </p>
                                
                                <a href="/buildings/building/<%= building.id %><%= isAdminPreview ? '?viewAs=admin' : '' %>" class="btn btn-primary w-100 shadow-sm">
                                    Otwórz budynek
                                </a>
                            </div>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <div class="col-12 text-center mt-5">
                    <div class="alert alert-info">
                        <i class="bi bi-info-circle me-2"></i> Obecnie nie masz przypisanych żadnych budynków.
                    </div>
                </div>
            <% } %>
        </div>
        <% } %>
    </div>

    <script src="/js/bootstrap.bundle.min.js"></script>
</body>
</html>